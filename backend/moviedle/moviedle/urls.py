from django.contrib import admin
from django.urls import path
from moviedle_API import views

urlpatterns = [
    # path('admin/', admin.site.urls), # Descomentar cuando se requiera las INSTALLED_APPS de settings.py
]
